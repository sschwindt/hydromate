"""Optional Streamlit-based graphical configuration editor for hydromate.

Install with the ``gui`` extra (``pip install -e ".[gui]"``) and launch with the
``hydromate-gui`` console script (see :mod:`hydromate.gui.launch`).
"""
